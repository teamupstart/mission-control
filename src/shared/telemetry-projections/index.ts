import type { TelemetryMetricDefinition } from "../telemetry-catalog.ts";
/** Phase 6 owns analytical instruments here; no source definitions or migrations. */
export const ANALYTICAL_METRICS: TelemetryMetricDefinition[] = [];
